# load quant table into multigraph object

# where a link is null, calculate the distance of the shortest path through the existing links. 

# add a link between those nodes with that distance-weight

# for each null link

# save back to graphml object and to sql table

